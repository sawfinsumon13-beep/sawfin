RewriteEngine On

# Serve .html files for clean URLs (e.g. /blog -> blog.html)
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteCond %{REQUEST_FILENAME}.html -f
RewriteRule ^(.+)$ $1.html [L,QSA]

# Default to index
DirectoryIndex index.html
