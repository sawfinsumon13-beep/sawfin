name: sawfin777
description: A mobile-first demo casino-style entertainment app using virtual coins only.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.9.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cloud_firestore: ^6.6.0
  cupertino_icons: ^1.0.8
  firebase_auth: ^6.5.4
  firebase_core: ^4.11.0
  google_fonts: ^8.1.0

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
